#include "main.h"

/**
 *add - returns the sum
 *Return: 0 (success)
 *@a: are number input parameter to the function
 *@b: is thr second input
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
