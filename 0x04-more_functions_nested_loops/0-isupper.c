#include <stdio.h>
#include "main.h"

/**
 * _isupper - entry point
 * @c: is the character to check for
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z')
}
